
version: 2

models:
    - name: fct_dbt_model_1
      description: "sample fact dbt model"
      columns:
          - name: id
            description: "primary key id"
            tests:
                - unique
                - not_null
